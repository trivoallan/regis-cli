# Active Context

## Current Objective
Bootstrap the image analysis project for {{ cookiecutter.project_name }}.

## Next Steps
- Define custom scorecard rules in `scorecards/default.yaml`.
- Configure the GitHub workflow in `.github/workflows/analyze.yml` with the correct image URL.
