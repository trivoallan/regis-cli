[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
regis-cli = {editable = true, path = "."}

[dev-packages]
pytest = ">=7.4"
pytest-cov = ">=4.1"
responses = ">=0.24"

[requires]
python_version = "3"
